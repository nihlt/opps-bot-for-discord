DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=
ADMIN_DISCORD_USER_IDS=
NOTION_TOKEN=
NOTION_DATABASE_ID=
NOTION_FEED_DATABASE_ID=
GEMINI_MODEL=
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=
GOOGLE_APPLICATION_CREDENTIALS=
SCRAPE_CONCURRENCY=
# Optional, ad-hoc use only -- unset for normal daily runs. See
# docs/architecture.md#which-items-actually-get-posted-digest_lookback_days
DIGEST_LOOKBACK_DAYS=
# Optional -- both unset means the admin cost DM shows token counts only,
# no $ estimate. Look up the current Vertex AI price for GEMINI_MODEL
# (per 1M tokens) before setting these; not filled in by default since
# pricing isn't something to guess at. See docs/architecture.md.
GEMINI_INPUT_PRICE_PER_1M_TOKENS=
GEMINI_OUTPUT_PRICE_PER_1M_TOKENS=
